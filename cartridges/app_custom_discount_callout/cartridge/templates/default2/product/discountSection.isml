<iscontent>
    <div>
        ${'Test ' + pdict.discountPercentage + ' % OFF'}
    </div>
    
    <iscomment> <isif condition="${!empty(pdict.discountPercentage)}">
        <div class="discount-percentage">
            ${Resource.msgf('best.deal.label', 'app_custom_discount_callout', null, pdict.discountPercentage)}
        </div>
    </isif> </iscomment>
</iscontent>